/// Available output data types.
enum OutputType {
  /// Local heat map processing output, corresponds to `localRenderCallback`.
  localRender,

  /// Raw data output, corresponds to `dataCallback`:
  data,
}
